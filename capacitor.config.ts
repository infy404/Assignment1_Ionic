import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Assignment1_Calculator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
